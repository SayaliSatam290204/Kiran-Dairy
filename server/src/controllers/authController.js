export const authController = {
  login: (req, res) => {
    res.json({ message: 'Login controller to be implemented' });
  },

  register: (req, res) => {
    res.json({ message: 'Register controller to be implemented' });
  }
};
